<div id="page">

<div id="main" class="aui-page-panel">

<div id="main-header">

<div id="breadcrumb-section">

1.  [Tech](index.html)
2.  [Tech Home](Tech-Home_33041.html)

</div>

# <span id="title-text"> Tech : Tip & Tech </span>

</div>

<div id="content" class="view">

<div class="page-metadata">

Created by <span class="author"> Dongwook Han</span>, last modified on 11월 14, 2022

</div>

<div id="main-content" class="wiki-content group">

# 서블릿 2.5, 3.0 비교

## 서블릿 정의

### Servlet 2.5

- web.xml servlet 정의

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  <servlet>
    <servlet-name>myServlet</servlet-name>
    <servlet-value>my.server.side.stuff.MyAwesomeServlet</servlet-value>
    <init-param>
      <param-name>configFile</param-name>
      <param-value>config.xml</param-value>
    </init-param>
  </servlet>

  <servlet-mapping>
    <servlet-name>myServlet</servlet-name>
    <url-pattern>/path/to/my/servlet</url-pattern>
  </servlet-mapping>
  ```

  </div>

  </div>

- filter정의

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  <filter>
    <filter-name>myFilter</filter-name>
    <filter-class>my.server.side.stuff.MyAwesomeFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>myFilter</filter-name>
    <url-pattern>/path/to/my/filter</url-pattern>
  </filter-mapping>
  ```

  </div>

  </div>

- listener 정의

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  <listener>
    <listener-class>my.server.side.stuff.MyAwesomeListener</listener-class>
  </listener>
  ```

  </div>

  </div>

- 하나의 web.xml 을 가짐

### Servlet 3.0

- web.xml 에 정의하지 않고 annotation 으로 정의할 수 있음(선택)

- java servlet 코드 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @WebServlet(name="myServlet", urlPatterns={"/path/to/my/servlet"}, initParams={@InitParam(name="configFile", value="config.xml")})
  public class MyAwesomeServlet extends HttpServlet {...}
  ```

  </div>

  </div>

- filter 정의

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @ServletFilter(name="myFilter", urlPatterns{"/apth/to/my/filter"})
  public class MyAwesomeFilter implementes Filter{...}
  ```

  </div>

  </div>

- listener 정의

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @WebServletContextlistener
  public class MyAwesomeListener implements ServletContextListener {...}
  ```

  </div>

  </div>

- 웹 서비스를 제공하는 라이브러리나 프레임워크의 로딩하는 jar마다 web-fragment.xml 을 META-INF디렉토리에 정의됨. servlets, filters 등 정의됨.

- Dynamic registration of servlet, filters and listeners정의

  - ServletContetListener에 서블릿, 필터, 리스터를 동적으로 추가할 수 있다.

  - addServlet(), addFilter() addListener() 메소드 지원함

- Asynchronous 지원

  - 서블릿 2.5 에서는 request가 들어와서 response반환할 때까지 하나의 스레드 차지함

  - 서블릿 3.0에서는 응답을 보내기 전에 다른 스레드로 delegate하고 service()종료할 수 있음. 비동기 방식 지원 가능

  - 예제

    <div class="code panel pdl" style="border-width: 1px;">

    <div class="codeContent panelContent pdl">

    ``` syntaxhighlighter-pre
    @WebServlet(name="myServlet", urlPatterns={"/mySloqProcess"}, asyncSupported=true) // asyncSupported MUST be specified for servlets that support asynchronous processing
    public class MyAwesomeServlet extends HttpServlet {
      @Override
      public void doGet(HttpServletRequest request, HttpServletResponse response) {
        // an AsyncContext is created, now the response will be eompleted
        // not when doGet finalizes its excuting, but when myAsuncContext.complete() is called
        AsyncContext myAsyncContext = request.startAsync(request, response);
        //...
        
        // myasuncContext is passed to another thread
        delegateExecutionToProcessingThread(myasyncContext);
        
        // done, not this thread is free to serve another request
      }
    }

    //.. and somewhere in another part of the code
    public class MyProcessingObject {
      public void doslowProcess() {
        //...
        runSlowProcess();
        myasyncContext.complete(); //request is now completed
        
        //...
      }
    }
    ```

    </div>

    </div>

- File upload

  1.  jsp정의 : form-encoding? multipart/form-data

  2.  Servlet request 파일 처리 메소드

      - public Colection\<Part\> getPart()

      - public Part getPart(String name)

      - @Multi partCofig 파일 업로드 시 파일 저장 위치, 크기 지정

- Etcs

  - HttpOnly Cookie

  - Session tracking

  - EJB in WAR

# Eclipse에 lombok설정

- plugin 이 설치되지 않는 상황에서 lomok.jar만 있을 경우 다음과 같이 설정

  - eclilpse.ini파일에 다음 항목 추가(맨 아래에)

    - -javaagent:c:\eclipse\bin\eclipse\lombok.jar

# 프로젝트 환경별 profille 사용

## Maven profile 설정 파일 사용

- src/main/resources디렉토리에 다음과 같이 설정

  1.  profiles 디렉토리 생성

  2.  profiles 디렉토리 밑에 환경별로 디렉토리 생성

      - dev, test, prod 등

  3.  maven pom.xml에 \<profile\>추가

      <div class="code panel pdl" style="border-width: 1px;">

      <div class="codeContent panelContent pdl">

      ``` syntaxhighlighter-pre
      <project>
        ...
        <profiles>
          <profile>
            <id>local</id>
            <properties>
              <env>local</env>
            </properties>
            <activation>
              <activeByDefault>true</activeDefault>
            </activation>
          </profile>
          <profile>
            <id>dev</id>
            <properties>
              <env>dev</env>
            </properties>
          </profile>
          <profile>
            <id>test</id>
            <properties>
              <env>test</env>
            </properties>
          </profile>
          <profile>
            <id>prod</id>
            <properties>
              <env>prod</env>
            </properties>
          </profile>
          ...
      ```

      </div>

      </div>

  4.  pom.xml의 \<build\> 항목에 \<resource\> 추가

      <div class="code panel pdl" style="border-width: 1px;">

      <div class="codeContent panelContent pdl">

      ``` syntaxhighlighter-pre
      <project>
        ...
        <build>
          ...
          <resources>
            <resource>
              <directory>src/main/resources</directory>
              <filtering>true</filtering>
              <excludes>
                <exclude>profiles/**/**</exclude>
              </excludes>
            </resource>
            <resource>
              <directory>src/main/resources/profiles/${env}</directory>
            </resource>
            ...
        </build>
      ```

      </div>

      </div>

- maven 기타 기능

  - outputDirectory에 resource로 지정한 경로의 파일을 복사,붙여 넣어줌

    <div class="code panel pdl" style="border-width: 1px;">

    <div class="codeContent panelContent pdl">

    ``` syntaxhighlighter-pre
    <pluginManagement>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>2.6</version>
          <executions>
            <execution>
              <id>copy-utilSrc</id>
              <phase<validate</phase>
              <goals>
                <goal>copy-resources</goal>
              </goals>
              <configuration>
                <outputDirectory>${basedir}/target/classes</outputDirectory>
                <resources>
                  <resource>
                    <directory>src/main/resources/utilSrc</directory>
                    <filtering>true</filtering>
                    <includes>
                      <include>**/*</include>
                    </includes>
                  </resource>
                </resources>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <!-- web.xml 환경별 복사 -->
        <plugin>
          <groupId>org.apache.maven.plugin</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <configuration>
            <webXml>src/main/resources/profiles/${env}/web.xml</webXml>
          </configuration>
        </plugin>
        ...
    </pluginManagement>
    ```

    </div>

    </div>

## Spring profile사용하기

### Bean에서 @Profile 사용

- 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @Component
  @Profile("dev")
  public class DevdataSourceConfig{
  .... }
  ```

  </div>

  </div>

- @Profile("!dev") 사용시 dev외에 모두 라는 의미임

### XML로 profile선언

- 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  <beans profile="dev">
    <bean id="devDataSourceConfig" class="...">
  </beans>
  ```

  </div>

  </div>

### 프로필 설정 방법

#### WebApplicationInitializer interface를 통해 사용

- 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @Configuration
  public class MywebApplicationInitializer implements WebApplicationInitializer {
    @Override
    public void onStartUp(ServletContext servletContext) throws ServletException {
      servletContext.setInitParameter("spring.profiles.active","dev");
    }
    ...
  }
  ```

  </div>

  </div>

#### ConfigurableEnvironment를 통한 설정

- 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @Autowired
  private ConfigurableEnvironment env;
  ...
  env.setActiveProfiles("someProfile");
  ```

  </div>

  </div>

#### web.xml 컨텍스트 매개변수

- 예제

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  <context-param>
    <param-name>spring.profiles.active</param-name>
    <param-value>dev</param-value>
  </context-param>
  ```

  </div>

  </div>

#### JVM시스템 매개변수 설정

<div class="code panel pdl" style="border-width: 1px;">

<div class="codeContent panelContent pdl">

``` syntaxhighlighter-pre
-Dspring.profiles.active=dev
```

</div>

</div>

#### 환경변수로 설정

<div class="code panel pdl" style="border-width: 1px;">

<div class="codeContent panelContent pdl">

``` syntaxhighlighter-pre
EXPORT spring_profiles_active=dev
```

</div>

</div>

#### Maven profiles

<div class="code panel pdl" style="border-width: 1px;">

<div class="codeContent panelContent pdl">

``` syntaxhighlighter-pre
<profiles>
  <profile>
    <id>dev</id>
    <activation>
      <activeByDefault>true</activeByDefault>
    </activation>
    <properties>
      <spring.profiles.active>dev</spring.profiles.active>
    </properties>
  </profile>
  <profile>
    <id>test</id>
    <properties>
      <spring.profiles.active>test</spring.profiles.active>
    </properties>
  </profile>
</profiles>
```

</div>

</div>

#### resource filtering활성화

<div class="code panel pdl" style="border-width: 1px;">

<div class="codeContent panelContent pdl">

``` syntaxhighlighter-pre
<build>
  <resources>
    <resource>
      <directory>src/main/resources</directory>
      <filtering>true</filtering>
    </resource>
  </resources>
  ...
</build>
```

</div>

</div>

#### Maven 매개변수 사용

- mvn clean package -Pprod

#### 테스트의 @activeProfile

- 테스트시 @ActiveProfile사용하여 특정 프로파일 활성화 가능

- @ActiveProfile("dev")

### 프로파일별 우선순위

1.  web.xml의 컨텍스트 매개변수

2.  WebApplicationInitializer

3.  JVM시스템 매개변수

4.  환경변수

5.  Maven프로필

### 프로파일 가져오는 법

- Spring의 active profile 을 사용하기 위해 @Profile사용

- 사용환경

  - spring.active.profile사용ㅎ하여 적용

#### 환경 개체를 삽입하여 활성 프로파일 억세스

<div class="code panel pdl" style="border-width: 1px;">

<div class="codeContent panelContent pdl">

``` syntaxhighlighter-pre
public class ProfileManager {
  @Autowired
  private Environment environment;
  
  public void getActiveProfiles() {
    for(String profileName: environment.getActiveProfiles()) {
      System.out.println("active profile : " + profileName);
    }
  }
}
```

</div>

</div>

#### spring.active.profile 사용

<div class="code panel pdl" style="border-width: 1px;">

<div class="codeContent panelContent pdl">

``` syntaxhighlighter-pre
@Value("${spring.profiles.active}")
private String activeProfile;
```

</div>

</div>

- profile이 설정이 안 되었을 때 IllegalArgument 오류 발생

- 기본값을 주기 위해 설정(빈문자열 설정)

  <div class="code panel pdl" style="border-width: 1px;">

  <div class="codeContent panelContent pdl">

  ``` syntaxhighlighter-pre
  @Value("${spring.profiles.active}")
  private String activeProfile;
  ```

  </div>

  </div>

</div>

</div>

</div>

<div id="footer" role="contentinfo">

<div class="section footer-body">

Document generated by Confluence on 4월 05, 2026 18:00

<div id="footer-logo">

[Atlassian](http://www.atlassian.com/)

</div>

</div>

</div>

</div>
